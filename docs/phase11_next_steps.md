# Phase 11 後の拡張候補

Phase 11の高次元入力モデル実装・benchmark・documentationは完了扱いとし、以降は独立した拡張として管理します。

優先候補は、連続高次元 `optimize_acqf` benchmark、REMBO / BAxUS / TuRBOなど探索戦略、Joint modelのconditioning / fantasize検証、VAE samplingと数値安定性、実データbenchmarkです。
